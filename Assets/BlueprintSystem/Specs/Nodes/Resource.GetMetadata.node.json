{
  "schemaVersion": "0.1",
  "typeId": "Resource.GetMetadata",
  "title": "Get Resource Metadata",
  "category": "Resource",
  "description": "Reads one metadata value from the generated Resource Blueprint registry, or returns the metadata JSON when key is empty.",
  "executor": "Resource.GetMetadata",
  "inputs": [
    { "id": "resourceType", "kind": "value", "type": "string", "required": true, "source": "propertyOrConnection" },
    { "id": "resourceName", "kind": "value", "type": "string", "required": true, "source": "propertyOrConnection" },
    { "id": "key", "kind": "value", "type": "string", "required": false, "source": "propertyOrConnection" }
  ],
  "outputs": [
    { "id": "value", "kind": "value", "type": "string" }
  ],
  "properties": [
    { "id": "resourceType", "type": "string", "required": true },
    { "id": "resourceName", "type": "string", "required": true },
    { "id": "key", "type": "string", "required": false, "defaultValue": "" }
  ],
  "tags": ["resource", "metadata"]
}
