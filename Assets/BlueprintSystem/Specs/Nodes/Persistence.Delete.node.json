{
  "schemaVersion": "0.1",
  "typeId": "Persistence.Delete",
  "title": "Delete Persistent Variables",
  "category": "Persistence",
  "description": "Deletes saved persistent variables for the current BlueprintRunner.",
  "executor": "Persistence.Delete",
  "inputs": [
    { "id": "execIn", "kind": "exec" },
    { "id": "slot", "kind": "value", "type": "string", "required": false, "source": "propertyOrConnection" }
  ],
  "outputs": [
    { "id": "deleted", "kind": "exec" },
    { "id": "missing", "kind": "exec" },
    { "id": "failed", "kind": "exec" },
    { "id": "error", "kind": "value", "type": "string" }
  ],
  "properties": [
    { "id": "slot", "type": "string", "required": false, "defaultValue": "" }
  ],
  "tags": ["persistence", "delete", "variables", "disk"]
}
