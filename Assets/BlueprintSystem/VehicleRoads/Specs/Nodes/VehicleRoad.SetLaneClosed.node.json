{
  "schemaVersion": "0.1",
  "typeId": "VehicleRoad.SetLaneClosed",
  "title": "VehicleRoad Set Lane Closed",
  "category": "VehicleRoads",
  "description": "Closes or reopens a lane at runtime.",
  "executor": "VehicleRoad.SetLaneClosed",
  "inputs": [
    { "id": "execIn", "kind": "exec" },
    { "id": "target", "kind": "value", "type": "Binding<VehicleRoadSubsystem>", "required": true, "source": "propertyOrConnection" },
    { "id": "laneId", "kind": "value", "type": "string", "required": true, "source": "propertyOrConnection" },
    { "id": "closed", "kind": "value", "type": "bool", "required": true, "source": "propertyOrConnection" }
  ],
  "outputs": [
    { "id": "execOut", "kind": "exec" }
  ],
  "properties": [
    { "id": "target", "required": true, "type": "Binding<VehicleRoadSubsystem>" },
    { "id": "laneId", "required": false, "type": "string", "defaultValue": "" },
    { "id": "closed", "required": false, "type": "bool", "defaultValue": false }
  ]
}
